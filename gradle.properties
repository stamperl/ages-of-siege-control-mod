# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache.
org.gradle.configuration-cache=false

# Fabric Properties
# These versions match the Ages Of Siege pack target.
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.18.4
loom_version=1.6-SNAPSHOT

# Mod Properties
mod_version=0.1.0
maven_group=com.stamperl
archives_base_name=ages-of-siege-control

# Dependencies
fabric_version=0.92.7+1.20.1
